@model Inscripciones.Models.inscripciones

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>inscripciones</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="fecha" class="control-label"></label>
                <input asp-for="fecha" class="form-control" />
                <span asp-validation-for="fecha" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="alumnosid" class="control-label"></label>
                <select asp-for="alumnosid" class="form-control" asp-items="ViewBag.alumnosid"></select>
                <span asp-validation-for="alumnosid" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="carreraid" class="control-label"></label>
                <select asp-for="carreraid" class="form-control" asp-items="ViewBag.carreraid"></select>
                <span asp-validation-for="carreraid" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
